name: Ordinary iTests

on:
  workflow_call:

jobs:

  ordinary-itests:
    name: iTests ${{ matrix.name }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - file: test_itde_manager.py
            name: ITDE Mgr
            extra_param: ""
          - file: test_sagemaker_extension_wrapper.py
            name: SME
            extra_param: ""
          - file: test_transformers_extension_wrapper.py
            name: TE
            extra_param: ""
          - file: itde_mgr_in_container/test_itde_connect.py
            name: ITDE Connect
            extra_param: ""
          - file: itde_mgr_in_container/test_itde_external.py
            name: ITDE External
            extra_param: ""
          - file: itde_mgr_in_container/test_itde_recreation_after_take_down.py
            name: ITDE Recreate after take-down
            extra_param: ""
          - file: itde_mgr_in_container/test_itde_recreation_without_take_down.py
            name: ITDE Recreate w/o take-down
            extra_param: ""
          - file: itde_mgr_in_container/test_itde_stop_and_restart.py
            name: ITDE Stop / Restart
            extra_param: ""
          - file: itest_slc.py
            name: SLC Pip Gzip
            extra_param: "--package-manager pip --compression-strategy gzip"
          - file: itest_slc.py
            name: SLC Conda
            extra_param: "--package-manager conda --compression-strategy gzip"
          - file: itest_slc.py
            name: SLC Pip uncompressed
            extra_param: "--package-manager pip --compression-strategy none"
          - file: itest_slc.py
            name: SLC BFS TLS
            extra_param: "--package-manager pip --compression-strategy none --bucketfs-protocol https"
          - file: test_cloud_storage.py
            name: CSE
            extra_param: ""

    steps:

      - name: SCM Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          large-packages: false
          haskell: true

      - name: Free disk space by removing large directories
        run: |
          sudo rm -rf /usr/local/graalvm/
          sudo rm -rf /usr/local/share/powershell
          sudo rm -rf /usr/local/share/chromium
          sudo rm -rf /usr/local/lib/node_modules

      - name: Setup Python & Poetry Environment
        uses: exasol/python-toolbox/.github/actions/python-environment@v5
        with:
          poetry-version: "2.3.0"

      - name: Install All Optional Dependencies (aka. "extras")
        run: poetry install --all-extras

      - name: Allow unprivileged user namespaces
        run: |
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0

      - name: Pytest
        run: poetry run -- pytest -rA --setup-show test/integration/ordinary/${{ matrix.file }} ${{matrix.extra_param}}
